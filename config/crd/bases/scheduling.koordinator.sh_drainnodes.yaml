---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.9.0
  creationTimestamp: null
  name: drainnodes.scheduling.koordinator.sh
spec:
  group: scheduling.koordinator.sh
  names:
    kind: DrainNode
    listKind: DrainNodeList
    plural: drainnodes
    shortNames:
    - dn
    singular: drainnode
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - description: The phase of DrainNode
      jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    - jsonPath: .spec.ttl
      name: TTL
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: DrainNode is the Schema for the resourcepolicies API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: DrainNodeSpec defines the desired state of DrainNode
            properties:
              confirmState:
                description: ConfirmState indicates the confirmation state.
                type: string
              cordonPolicy:
                description: CordonPolicy decides how to cordon drained nodes
                properties:
                  labels:
                    additionalProperties:
                      type: string
                    type: object
                  mode:
                    type: string
                type: object
              migrationPolicy:
                description: MigrationPolicy decides how the migrations progress
                properties:
                  mode:
                    type: string
                  waitDeadline:
                    type: string
                type: object
              nodeName:
                description: Node name that needs to be drained
                type: string
              ttl:
                description: TTL controls the DrainNode timeout duration.
                type: string
            type: object
          status:
            description: DrainNodeStatus defines the observed state of DrainNode
            properties:
              conditions:
                description: Conditions describes the errors, if any.
                items:
                  properties:
                    lastProbeTime:
                      description: Last time we probed the condition.
                      format: date-time
                      nullable: true
                      type: string
                    lastTransitionTime:
                      description: Last time the condition transitioned from one status
                        to another.
                      format: date-time
                      nullable: true
                      type: string
                    message:
                      description: Human-readable message indicating details about
                        last transition.
                      type: string
                    reason:
                      description: Unique, one-word, CamelCase reason for the condition's
                        last transition.
                      type: string
                    status:
                      description: Status is the status of the condition. Can be True,
                        False, Unknown.
                      type: string
                    type:
                      description: Type is the type of the condition.
                      type: string
                  required:
                  - status
                  - type
                  type: object
                type: array
              phase:
                description: Phase indicates what stage the DrainNode is in, such
                  as Waiting, Running, and Succeed.
                type: string
              pogMigrationSummary:
                additionalProperties:
                  format: int32
                  type: integer
                description: PodMigrationSummary provides summary information of migrations
                type: object
              pogMigrations:
                description: PodMigrations indicates the migration status of pods
                  on the node.
                items:
                  properties:
                    chartedAfterDrainNodeConfirmed:
                      description: The startTime of the PodMigration
                      type: boolean
                    namespace:
                      description: Namespace where the pod is in
                      type: string
                    phase:
                      description: The phase of the PodMigration
                      type: string
                    podMigrationJobName:
                      description: The name of the PodMigrationJob.
                      type: string
                    podMigrationJobPaused:
                      description: If PodMigrationJob paused
                      type: boolean
                    podMigrationJobPhase:
                      description: Status of the PodMigrationJob.
                      type: string
                    podName:
                      description: The name of the Pod.
                      type: string
                    reservationName:
                      description: The name of the Reservation.
                      type: string
                    reservationPhase:
                      description: Status of the Reservation.
                      type: string
                    startTimestamp:
                      description: The startTime of the PodMigration
                      format: date-time
                      type: string
                    targetNode:
                      description: The target node for pod migration.
                      type: string
                    uid:
                      description: The UID of the Pod
                      type: string
                  type: object
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
