
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.2.9
  creationTimestamp: null
  name: recommendationprofiles.autoscaling.alibabacloud.com
spec:
  group: autoscaling.alibabacloud.com
  names:
    kind: RecommendationProfile
    listKind: RecommendationProfileList
    plural: recommendationprofiles
    shortNames:
    - redprofile
    singular: recommendationprofile
  scope: Cluster
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: RecommendationProfileSpec defines the desired state of RecommendationProfile
            properties:
              controllerKind:
                description: ControllerKind indicates which controller the pod managed
                  by when RuleType equals "controller".
                items:
                  type: string
                type: array
              disabledNamespaces:
                description: DisabledNamespaces indicates which namesapces the RecommendationProfile
                  will not generate recommendation cr for and "*" represents all namespaces.
                items:
                  type: string
                type: array
              enabledNamespaces:
                description: EnabledNamespaces indicates which namespaces the RecommendationProfile
                  will generate recommendation cr for and "*" represents all namespaces.
                items:
                  type: string
                type: array
              type:
                description: Type indicates which pattern the pod managed by. It can
                  be a controller like deployment, statefulset, etc . Of course, it
                  may not be managed by any controllers and only is a pod.
                type: string
              workloadRefLabelKeys:
                description: WorkloadRefLabelKeys indicates which pod's label key
                  can distinguish from other type of pod.
                items:
                  type: string
                type: array
            type: object
          status:
            description: RecommendationProfileStatus defines the observed state of
              RecommendationProfile
            type: object
        type: object
    served: true
    storage: true
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
